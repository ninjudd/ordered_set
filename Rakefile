# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/ordered_set.rb'

Hoe.new('OrderedSet', OrderedSet::VERSION) do |p|
  # p.rubyforge_name = 'OrderedSetx' # if different than lowercase project name
  p.developer('Justin Balthrop', 'code@justinbalthrop.com')
end

# vim: syntax=Ruby
